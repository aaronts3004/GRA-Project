#include <stddef.h>
#include <stdlib.h>
#include <stdio.h>
#include <stdint.h>
#include "prim.h"

/*  Function to handle edge cases when setting version in the main implementation
    Inputs: 
        - 'cur_ver': the current version chosen by the user 
        - 'n': input number of primes
    Return: the final version which will used in the main program
*/
int handle_version(int cur_ver, size_t n);


